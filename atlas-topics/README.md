# Atlas Topics improvement

## Run

```bash
python main.py --dataset "your-dataset-name" --indexed-field "YourField"
```

e.g.

```bash
python main.py --dataset ai-policy-recommendations --indexed-field Details
```